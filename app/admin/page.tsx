import { AdminConsole } from "../_components/admin-console";

export const metadata = {
  title: "Administration | JuridiqueSN",
};

export default function AdminPage() {
  return <AdminConsole />;
}
